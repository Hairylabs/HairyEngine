# Copy to .env and fill in. .env is gitignored.
#
# Private key of the deployer wallet (no 0x prefix needed; Hardhat accepts both).
# NEVER commit this file with a real key.
PRIVATE_KEY=

# Optional: Etherscan-style API key for contract verification on networks that support it.
ETHERSCAN_API_KEY=
